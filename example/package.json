{
  "name": "chat-state-cloudflare-do-example",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix .. run build",
    "predeploy": "npm --prefix .. run build",
    "pretypecheck": "npm --prefix .. run build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chat-state-cloudflare-do": "file:.."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.106.0"
  }
}
